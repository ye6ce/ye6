[build]
# This runs the vite build script from your package.json
command = "npm run build"
# This tells Netlify to serve the folder Vite creates
publish = "dist"

[[redirects]]
# This handles React routing so you don't get 404s on refresh
from = "/*"
to = "/index.html"
status = 200